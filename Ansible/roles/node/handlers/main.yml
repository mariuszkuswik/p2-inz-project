---
# 
- name: Docker Swarm - Initialization
  command: "docker swarm join --advertise-addr {{ control_plane_ip }}:2377 --token {{ tmp_swarm_join_token_worker.stdout_lines }}"
  when: swarm_check_result.stdout is not search('active') 
  register: swarm_init_result
  listen: "join_swarm"